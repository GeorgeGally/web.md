{
  "name": "webmd",
  "version": "0.1.0",
  "description": "web/md — strips websites to pure semantic markdown, like bots see them",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite build --watch",
    "build": "node build.js",
    "build:vite": "vite build",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "jsdom": "^29.1.1",
    "vite": "^6.3.0",
    "vitest": "^3.2.0"
  },
  "dependencies": {
    "@mozilla/readability": "^0.5.0",
    "marked": "^15.0.0",
    "turndown": "^7.2.0"
  }
}
